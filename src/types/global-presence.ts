export interface GlobalPresenceItem {
  image: string;
  title: string;
}

export interface GlobalPresenceData {
  eyebrow: string;
  heading: string;
  description: string;
  items: GlobalPresenceItem[];
}
