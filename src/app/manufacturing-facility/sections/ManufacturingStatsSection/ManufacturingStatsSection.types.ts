import  { StatCardProps }  from "@/components/ui/StatCard/StatCard.types";

export interface ManufacturingStatsSectionProps {
  eyebrow: string;
  title: string;
  description: string;
  stats: StatCardProps[];
}